### Firefox proxy switcher

Firefox extension that switches proxy state between "No proxy" and "Manual" by clicking toolbar icon

* When proxy is ON - toolbar icon is green
* When proxy is OFF - toolbar icon is red
